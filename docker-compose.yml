version: "3.8"

services:
  vite-react-app:
    container_name: leaderboard_frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5040:80"
    environment:
      - .env
